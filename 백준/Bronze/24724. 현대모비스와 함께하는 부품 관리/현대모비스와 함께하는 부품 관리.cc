#include <iostream>
#include <string>
#include <cmath>
#include <stack>
using namespace std;

int main(void) {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int T;
	cin >> T;
	
	for(int i=1; i<=T; i++) {
		int N;
		cin >> N;
		
		for(int i=0; i<=N; i++) {
			int a, b;
			cin >> a >> b;
		}
		cout << "Material Management " << i << "\nClassification ---- End!\n";
	}
	
	return 0;
}
